---
id: T-005-01-01
story: S-005-01
title: add-vercel-project-config
type: task
status: open
priority: high
phase: done
depends_on: [T-001-03-01]
---

## Context

Declare how the repo deploys: a Vercel config with the Next.js framework preset and `npm run build` as the build command, so deploy settings live in-repo.

_Advances: P3, E-005_

## Acceptance Criteria

- [ ] vercel.json (or equivalent project config) exists at repo root declaring the Next.js framework preset and `npm run build` as build command; config schema validates and `npx vercel build` resolves the build locally with no game/component files touched.
