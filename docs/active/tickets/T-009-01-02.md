---
id: T-009-01-02
story: S-009-01
title: wire-clay-tokens-into-tailwind-theme
type: task
status: open
priority: critical
phase: ready
depends_on: [T-009-01-01]
---

## Context

Replace the hand-rolled dark #0a0a0f/#ededf2 :root pair in app/globals.css with the kit's --clay-* tokens, bridged through @theme inline the same way the existing background/foreground pipeline already works, so the base palette becomes warm off-white/ink instead of neon-dark.

_Advances: P2_

## Acceptance Criteria

- [ ] app/globals.css imports the vendored kit file and --color-background/--color-foreground resolve to the kit's warm off-white/ink values (not #0a0a0f/#ededf2); `npm run build` succeeds and the rendered body background is the new light clay tone.
